import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  CircleContainer: {
    height: "350px",
    maxHeight: "90vw",
    width: "100%",
    position: "relative",
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
  },
}));
